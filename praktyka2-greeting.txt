CREATE FUNCTION SayHello(@firstName varchar(50), @lastName varchar(50))
RETURNS varchar(50)
AS
   BEGIN
      declare @result varchar(50) = 'Witaj '+ @firstName + ' ' + @lastName + '!';
      RETURN @result;
   END

select dbo.SayHello('Jan', 'Kowalski');
